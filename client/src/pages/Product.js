import { useState } from 'react';
import { getCategroy } from '../api/categoryAPI';

function Product() {
  const [category, setCategory] = useState({});

  return <div>HI im here. 나ㅏㅏㅏ는 상품페이지야</div>;
}

export default Product;
